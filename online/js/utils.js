export const peso=v=>new Intl.NumberFormat('en-PH',{style:'currency',currency:'PHP'}).format(Number(v||0));
export const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
export const fmtDate=v=>{if(!v)return '—';const d=new Date(v);return Number.isNaN(d.getTime())?'—':d.toLocaleDateString('en-PH',{month:'short',day:'numeric',year:'numeric'})};
export const remaining=v=>{if(!v)return 'No due date';const ms=new Date(v)-Date.now();if(ms<=0)return 'Due / overdue';const days=Math.floor(ms/86400000),hrs=Math.floor(ms%86400000/3600000),mins=Math.floor(ms%3600000/60000);return days?`${days}d ${hrs}h remaining`:`${hrs}h ${mins}m remaining`};
export const toast=msg=>{const el=document.getElementById('toast');el.textContent=msg;el.classList.add('show');clearTimeout(window.__t);window.__t=setTimeout(()=>el.classList.remove('show'),2600)};
async function decodeImage(file){if('createImageBitmap' in window)return createImageBitmap(file);const url=URL.createObjectURL(file);try{return await new Promise((resolve,reject)=>{const img=new Image();img.onload=()=>resolve(img);img.onerror=()=>reject(new Error('Could not read this image.'));img.src=url})}finally{setTimeout(()=>URL.revokeObjectURL(url),0)}}
export async function compressImage(file,maxBytes=5*1024*1024){if(!file.type.startsWith('image/')||file.size<=3*1024*1024)return file;const img=await decodeImage(file);let scale=Math.min(1,2000/Math.max(img.width,img.height));for(let q=.86;q>=.55;q-=.08){const c=document.createElement('canvas');c.width=Math.max(1,Math.round(img.width*scale));c.height=Math.max(1,Math.round(img.height*scale));c.getContext('2d').drawImage(img,0,0,c.width,c.height);const blob=await new Promise(r=>c.toBlob(r,'image/jpeg',q));if(blob&&blob.size<=maxBytes)return new File([blob],file.name.replace(/\.[^.]+$/,'.jpg'),{type:'image/jpeg'});scale*=.85}throw new Error('Image is still larger than 5 MB after compression.');}
