import { requireUser, ensurePortalAccount, sendError } from './_lib.js';export default async function handler(req,res){try{if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const {user,svc}=await requireUser(req);const acc=await ensurePortalAccount(user,svc);if(req.body?.action!=='password-set')return res.status(400).json({error:'Unknown action'});const u=await svc.from('portal_accounts').update({password_set:true,updated_at:new Date().toISOString()}).eq('auth_user_id',user.id);if(u.error)throw u.error;return res.status(200).json({ok:true});}catch(e){return sendError(res,e)}}
